# Исправление ошибки камеры на Windows 11

## Проблема
```
NotReadableError: Could not start video source
```

Ошибка возникала при попытке использовать камеру для сканирования QR-кодов на Windows 11.

## Внесенные изменения

### 1. Улучшен компонент QRScanner.tsx

**Файл:** `frontend/src/components/qr/QRScanner.tsx`

**Изменения:**
- ✅ Добавлена проверка разрешений камеры перед запуском
- ✅ Детальная обработка ошибок с конкретными сообщениями
- ✅ Понятные инструкции для пользователя при ошибках
- ✅ Улучшенные настройки камеры (aspectRatio, facingMode)
- ✅ Поддержка загрузки файлов как альтернатива камере

**Ключевые улучшения:**

```typescript
// Проверка разрешений перед запуском
const stream = await navigator.mediaDevices.getUserMedia({
  video: {
    facingMode: 'environment',
    width: { ideal: 1280 },
    height: { ideal: 720 }
  }
});

// Специфичная обработка ошибок Windows 11
if (error.name === 'NotReadableError') {
  setPermissionError('Камера используется другим приложением...');
}
```

### 2. Создана документация по устранению проблем

**Файл:** `CAMERA_TROUBLESHOOTING.md`

Подробное руководство с:
- Диагностикой проблем
- Пошаговыми инструкциями
- Настройками Windows 11
- Альтернативными решениями

### 3. Обновлен README.md

Добавлена секция в Troubleshooting со ссылкой на полную документацию.

### 4. Создана тестовая страница

**Файл:** `frontend/src/pages/test/CameraTestPage.tsx`

Тестовая страница для диагностики проблем с камерой:
- URL: `http://localhost:5173/test/camera`
- Показывает системную информацию
- Тестирует QR-сканер
- Отображает историю сканирования

## Как проверить исправление

### 1. Перезапустите frontend

```bash
cd frontend
npm run dev
```

### 2. Откройте тестовую страницу

Перейдите на: http://localhost:5173/test/camera

### 3. Проверьте работу камеры

1. Браузер запросит разрешение на доступ к камере - разрешите
2. Если камера работает - увидите видеопоток
3. Наведите на QR-код для тестирования сканирования

### 4. Если видите ошибку

Компонент теперь показывает:
- ❌ **Конкретную ошибку** (вместо технической)
- 💡 **Инструкции по исправлению** (раскрывающийся список)
- 🔧 **Шаги для решения проблемы**

## Типичные сценарии на Windows 11

### Сценарий 1: Камера занята другим приложением

**Ошибка:** "Камера используется другим приложением"

**Решение:**
1. Закройте Teams, Skype, Zoom
2. Перезагрузите браузер
3. ✅ Должно заработать

### Сценарий 2: Доступ запрещен в настройках Windows

**Ошибка:** "Доступ к камере запрещен"

**Решение:**
1. Win + I → Конфиденциальность и безопасность → Камера
2. Включите все переключатели
3. ✅ Должно заработать

### Сценарий 3: Доступ запрещен в браузере

**Ошибка:** "Доступ к камере запрещен"

**Решение:**
1. Нажмите на замок в адресной строке
2. Настройки сайта → Камера → Разрешить
3. Перезагрузите страницу
4. ✅ Должно заработать

### Сценарий 4: Камера не работает вообще

**Если ничего не помогает:**
- Используйте кнопку "Загрузить файл" в сканере
- Сфотографируйте QR-код на телефоне
- Загрузите фото в сканер
- ✅ Альтернативный способ работает всегда

## Проверка в production

### Важно: HTTPS требование

Браузеры требуют HTTPS для доступа к камере (кроме localhost).

**Для production:**
```nginx
server {
    listen 443 ssl;
    server_name olimpqr.example.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    location / {
        proxy_pass http://frontend:80;
    }
}
```

**Для локальной разработки:**
- ✅ `http://localhost:5173` - работает
- ✅ `http://127.0.0.1:5173` - работает
- ❌ `http://192.168.1.100:5173` - не работает (требуется HTTPS)

## Технические детали

### Проверяемые ошибки

| Код ошибки | Причина | Сообщение пользователю |
|------------|---------|------------------------|
| `NotAllowedError` | Пользователь запретил доступ | "Доступ к камере запрещен. Разрешите доступ в настройках браузера." |
| `NotFoundError` | Камера не найдена | "Камера не найдена. Проверьте подключение камеры." |
| `NotReadableError` | Камера занята | "Камера используется другим приложением. Закройте другие программы." |

### Настройки камеры

```typescript
{
  fps: 10,                          // Кадров в секунду
  qrbox: { width: 250, height: 250 }, // Область сканирования
  aspectRatio: 1.0,                 // Соотношение сторон
  showTorchButtonIfSupported: true, // Кнопка фонарика (мобильные)
}
```

## Дальнейшие улучшения

Возможные улучшения в будущем:

1. **Выбор камеры** - если несколько камер
2. **Настройки качества** - выбор разрешения
3. **PWA support** - работа оффлайн
4. **Автофокус** - для лучшего распознавания
5. **Зум** - для сканирования с расстояния

## Проверочный список

- [x] Компонент обрабатывает ошибки Windows 11
- [x] Показываются понятные сообщения об ошибках
- [x] Есть инструкции по исправлению
- [x] Альтернатива через загрузку файла
- [x] Документация создана
- [x] Тестовая страница работает
- [x] README обновлен

## Контакты

При проблемах создайте issue с информацией:
- Версия Windows: Win + R → `winver`
- Версия браузера: Настройки → О браузере
- Текст ошибки из консоли: F12 → Console
- Скриншот ошибки

---

**Дата исправления:** 2026-02-14
**Статус:** ✅ Исправлено и протестировано
